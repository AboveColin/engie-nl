__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
.DS_Store

# Live-test artefacts. Tokens and captured responses are written outside the
# working tree (~/.config/engie-nl/), so nothing here depends on these rules;
# they stay as a second line of defence against an older probe.py.
tests/fixtures/_live/
*.tokens.json
